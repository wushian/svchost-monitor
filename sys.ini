; ============================================================
; Process Memory Monitor — 設定檔
; 優先順序（低→高）：sys.ini < 環境變數 < 命令列參數
; 命令列覆蓋範例：
;   SvchostMonitor.exe --Monitor:ProcessName notepad --Monitor:MemThresholdGb 0.5
; ============================================================

[Monitor]
; 要監測的 Process 名稱（不含 .exe）
ProcessName=svchost
; 記憶體上限（GB），超過即 Kill
MemThresholdGb=2.0
; 檢查間隔（秒）
CheckIntervalSeconds=300
; 自訂備註，會顯示在 Telegram 通知中，留空則不顯示
Remark=

[Telegram]
; 從 @BotFather 取得
BotToken=6464836456:AAHvWUpPhw2MEe6G4E-3MfXls8kqqMCsGeg
; 目標 Chat ID（可用 @userinfobot 查詢）
ChatId= -4168837750
